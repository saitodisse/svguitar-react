# Tasks: Nut and Canvas Customization

## Goal

Add public controls for nut appearance and global SVG canvas positioning.

## Nut Props

- `nutStrokeWidth`
- `nutOffsetX`
- `nutOffsetY`
- `nutOpacity`
- `nutColor`

## Canvas Props

- `canvasOffsetX`
- `canvasOffsetY`

## Tasks

- [ ] Update the data model and API contract.
- [ ] Add props to `ChordDiagramProps` and style types.
- [ ] Add default values.
- [ ] Add tests for nut width, color, opacity, and offsets.
- [ ] Apply canvas offsets to all rendered diagram groups.
- [ ] Add Storybook controls and examples.
- [ ] Run `pnpm test:run`, `pnpm lint`, and `pnpm build`.
